-- Create audit table
CREATE TABLE emp_audit(
    audit_id INT AUTO_INCREMENT PRIMARY KEY,
    emp_id INT,
    action VARCHAR(20),
    action_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- BEFORE INSERT Trigger
DELIMITER //
CREATE TRIGGER before_emp_insert
BEFORE INSERT ON employee
FOR EACH ROW
BEGIN
    IF NEW.salary < 0 THEN
        SET NEW.salary = 0;
    END IF;
END //
DELIMITER ;

-- AFTER INSERT Trigger
DELIMITER //
CREATE TRIGGER after_emp_insert
AFTER INSERT ON employee
FOR EACH ROW
BEGIN
    INSERT INTO emp_audit(emp_id, action) VALUES(NEW.emp_id, 'INSERTED');
END //
DELIMITER ;

-- AFTER UPDATE Trigger
DELIMITER //
CREATE TRIGGER after_emp_update
AFTER UPDATE ON employee
FOR EACH ROW
BEGIN
    INSERT INTO emp_audit(emp_id, action) VALUES(NEW.emp_id, 'UPDATED');
END //
DELIMITER ;

-- AFTER DELETE Trigger
DELIMITER //
CREATE TRIGGER after_emp_delete
AFTER DELETE ON employee
FOR EACH ROW
BEGIN
    INSERT INTO emp_audit(emp_id, action) VALUES(OLD.emp_id, 'DELETED');
END //
DELIMITER ;

-- Test triggers
INSERT INTO employee VALUES(106,'Test',22,40000,1);
UPDATE employee SET salary=45000 WHERE emp_id=106;
DELETE FROM employee WHERE emp_id=106;
SELECT * FROM emp_audit;
